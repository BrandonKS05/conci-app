{
  "permissions": {
    "allow": [
      "Bash(*)",
      "Edit(*)",
      "Write(*)",
      "Read(*)"
    ],
    "deny": []
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys, json, re; data = json.load(sys.stdin); path = data.get('tool_input', {}).get('file_path', '') or data.get('tool_input', {}).get('path', ''); re.search(r'\\.env', path) and (print('BLOCKED: .env files are off-limits'), sys.exit(2))\""
          }
        ]
      }
    ]
  }
}
